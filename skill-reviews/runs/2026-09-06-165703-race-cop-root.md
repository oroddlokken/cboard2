---
skill: race-cop
mode: Quick
target: root
rev: fadb3ec
dirty: yes
reviewers: shared-mutable-state, check-then-act, lock-discipline, task-lifecycle
skipped: (none — Quick mode ran all 4 selected reviewers)
---

## Concurrency Results

### Red — Fix Now
Loss or silent corruption from an interleaving this code can have.

1. [ ] **`RemoteReader._states` has two unsynchronized writers** — the "poll" thread's `_adopt_origins`/`_apply` and the "remote" thread's `_rebuild` all mutate the same dict with no lock.
   `/Users/ove/git/cboard2/src/cboard2/remote.py:1029` (whole-dict swap in `_rebuild`), `:1095-1101` (`_adopt_origins`), `:1120-1128` (`_apply`) — stop `_adopt_origins`/`_apply` from writing `self._states` directly; have them compute and return path→patch mappings that only `_rebuild` (the single owner) folds in, instead of two independent writers racing a wholesale dict replacement.
   Runs at once: "poll" worker (`tui.py:889`, via `refresh_local→_adopt_origins`/`_apply`) vs "remote" worker (`tui.py:901`, via `read/prime→_rebuild`) — confirmed two distinct `@work` groups, no lock anywhere in `src/cboard2/` (grep confirmed by two reviewers). | Interleaving: `_adopt_origins` reads a pre-rebuild `state` at line 1099, `_rebuild` swaps `self._states` to a fresh dict at line 1029, then `_adopt_origins` writes `replace(stale_state, origin=url)` into the new dict at line 1101 — silently overwriting a freshly-fetched sha/PR/check reading with a stale-derived one, nothing raised, wrong until the next 300s remote cycle. A second branch of the same race (`_apply` at 1120-1128 reading/writing `self._states[path]` mid-loop while `_rebuild` swaps the dict underneath it) can also raise `KeyError` out of the "poll" thread. | Lens: `shared-mutable-state` + `check-then-act` (flagged by 2/4) — **[CONFLICT]** shared-mutable-state's fix removes the shared mutation ("have `_adopt_origins` return patches"); check-then-act's fix adds a lock around the same read-modify-write. Resolved per ladder rule 1 (removing the sharing beats guarding it): take shared-mutable-state's approach.

2. [ ] **`RemoteReader.forget_absent` snapshots then deletes against a dict that can be swapped underneath it** — a plain `del` racing `_rebuild`'s reassignment.
   `/Users/ove/git/cboard2/src/cboard2/remote.py:1450-1455` — capture `self._states` once at the top of `forget_absent` into a local variable and operate on that captured reference with `.pop(path, None)` instead of re-reading `self._states` (and re-raising `KeyError` via `del`) on every iteration; this survives a concurrent `_rebuild` swap without a lock.
   Runs at once: "poll" thread `Board.rescan→RemoteReader.forget_absent` (`board.py:96`) vs "remote" thread `read()→_rebuild` (`remote.py:1029`) — reachable even at startup because `Board.refresh`'s rescan-if-due check (`board.py:119-120`) and `Board.read_remote`'s rescan-if-empty check (`board.py:160-161`) can both fire on the first tick (see item 7). | Interleaving: the list comprehension snapshots "no longer live" paths against the *old* `_states`, then each `del self._states[path]` executes against whatever dict `self._states` now points to — either raising `KeyError` (path absent from the new dict) or silently deleting a still-live repo's just-fetched remote reading (path happens to coincide), with nothing reported in the second case. | Lens: `check-then-act`.

3. [ ] **A stale "poll" worker's `apply_rows` call can land after a fresher one and regress the table** — Textual's per-group `exclusive=True` cancels the asyncio *task*, not the executor thread already running the callable.
   `/Users/ove/git/cboard2/src/cboard2/tui.py:899` and `:910` — add `if self.is_cancelled: return` immediately before `self.call_from_thread(...)` in both `poll()` and `poll_remote()`.
   Runs at once: tick *N*'s "poll" worker vs tick *N+1*'s "poll" worker (`set_interval(self._refresh_interval, self.poll)`, `tui.py:884`) — confirmed independently against Textual's installed source: `Worker.cancel()` (`worker.py:416-421`) only calls `self._task.cancel()`; `_run_threaded` (`worker.py:284-326`) already handed the callable to `loop.run_in_executor(None, ...)`, and cancelling the wrapping task does not stop that executor thread — Textual's own docstring says so ("cancelled work may still be running", `worker.py:214`). `call_from_thread` (`app.py:1788`) has no cancellation check. | Interleaving: under a slow disk/large repo count, tick N's worker A keeps running past tick N+1's worker B; if A's `call_from_thread(self.apply_rows, rows)` marshals onto the UI thread after B's, `self._rows` (`tui.py:914`) regresses to A's older snapshot with nothing reporting it — self-corrects on the next successful tick (~2s later) but is a genuine silent regression while it lasts. | Lens: `lock-discipline`.

### Yellow — Should Address
Something stops or disappears, and a human resolves it after diagnosis.

4. [ ] **`ActivityReader.entries()` checks staleness then indexes without re-checking, racing a concurrent delete** — can throw `KeyError` out of either the "poll" thread or a UI-thread keypress handler.
   `/Users/ove/git/cboard2/src/cboard2/activity.py:132-133` — change `return self._entries[repo.path]` to `return self._entries.get(repo.path, [])`. Also fold `_entries`/`_read_at` into one dict keyed by path holding `(mtime, entries)` so the two related fields can never be written out of step (removes the separate mismatched-pair angle below).
   Runs at once: UI thread `entries()` (via `DetailScreen`/`ActivityScreen` on `enter`/`a` keypress, calling `Board.activity()→feed→entries` synchronously — *not* through a worker) vs "poll" thread `Board.rescan→ActivityReader.forget_absent` (`activity.py:169-170`). | Interleaving: `forget_absent` deletes the key between the UI thread's staleness check (line 132) and its dict index (line 133) → uncaught `KeyError`. Given Textual's default `exit_on_error=True` (confirmed at `worker.py:382-384`), the same check-then-index bug firing inside the "poll" worker's own call path (`Board.refresh→reader.latest()→entries()`, `board.py:128`) crashes the *whole app*, not just that tick. A second angle on the same dicts (`prime()`'s writes at `activity.py:113-115` vs `entries()`'s writes at `:135-137`) can also pair a fresh entries list with a stale `_read_at`, or vice versa, with no error. | Lens: `check-then-act` + `shared-mutable-state` (flagged by 2/4).

5. [ ] **No `@work(thread=True)` body catches exceptions, and Textual's `exit_on_error=True` default means any uncaught one crashes the whole app** — not the silent, permanent freeze the original findings described (validation correction: see note below).
   `/Users/ove/git/cboard2/src/cboard2/tui.py:889-899` (poll), `:901-910` (remote), `:1111-1119` (pull) — wrap each worker body in `try/except Exception`, log/notify (`self.notify(..., severity="error")`), and for `pull()` specifically use `try/finally` (not bare `try/except`) so `call_from_thread(self.pulled, path, outcome)` always fires and `self._pulling` is always released even on an unexpected exception — a bare except without the `finally` would leave `path` stuck in `self._pulling` forever per keypress `P` on that repo.
   Runs at once: any of the three `@work` thread bodies vs `set_interval`'s next tick (`tui.py:884`, `:887`) or the next `P` keypress — confirmed via Textual's `worker.py:369-384`: an uncaught `Exception` sets `WorkerState.ERROR` and, since `exit_on_error` defaults to `True` and is never overridden here, calls `app._handle_exception(...)`, which exits/crashes the app with a traceback. | Lens: `task-lifecycle` (flagged by 1/4; corrected — original findings claimed a silent, forever-stuck failure rather than an app crash).

6. [ ] **"pull" workers are spawned one per distinct repo path with no ceiling** — a burst of `P` keypresses across many repos has no cap on concurrent threads/subprocesses.
   `/Users/ove/git/cboard2/src/cboard2/tui.py:1093-1111` — bound concurrent "pull" workers with a semaphore or a fixed-size queue (e.g., cap at 4-8 in-flight, queue the rest) in `action_pull_default`/`pull()`.
   Runs at once: N distinct `P` keypresses across N distinct repos (dedup is per-path via `self._pulling`, `tui.py:868`, not per-count) — confirmed no cap exists; each spawns its own `@work(thread=True, group="pull")` thread (not exclusive, `tui.py:1111`), each capable of up to 4 subprocess calls at 180s timeout apiece (`pull.py:64-71`). | Lens: `task-lifecycle`.

### Green — Consider
Correct under this concurrency shape, a narrow window, or a constraint on raising the worker count. Fix when nearby.

7. [ ] **Both workers can rescan concurrently on the very first tick** — a duplicate filesystem walk, and the trigger that makes item 2 reachable at startup.
   `/Users/ove/git/cboard2/src/cboard2/board.py:119-120` and `:160-161` — guard `Board.rescan()` with a re-entry check (e.g., a simple flag or lock) so a concurrent caller waits rather than starting a second walk.
   Runs at once: "poll" thread's first `poll()` (`tui.py:883`) vs "remote" thread's first `poll_remote()` (`tui.py:886`), both invoked from `on_mount` moments apart, each independently satisfying its own "rescan if due/empty" check. | Lens: `check-then-act`.

8. [ ] **Two harmless idempotent redundant-write races on `RemoteReader`'s memoization dicts** — both reviewers who flagged these called them self-correcting; demoted from check-then-act's Medium vote per Rule A (an unguarded cache where both racing writers compute the same deterministic value).
   `/Users/ove/git/cboard2/src/cboard2/remote.py:1246-1252` (`_ancestry` memo) and `:1271` (`_origin_urls.update`) — no action required; if hardened, use `setdefault`-style atomic writes rather than a lock.
   Runs at once: "poll" thread (`_apply→_behind` / `_adopt_origins→_origins`) vs "remote" thread (`_apply→_behind` / `read/prime→_origins`) — both write the same equal/deterministic value; at most a redundant `git merge-base`/`get-url` subprocess call. | Lens: `shared-mutable-state` + `check-then-act` (flagged by 2/4).

9. [ ] **Confirmed harmless calibration cases — no action needed.**
   `/Users/ove/git/cboard2/src/cboard2/tui.py:1189` (`Board.config` — whole-object atomic pointer swap of a frozen dataclass, confirmed `frozen=True` in `config.py:76`; readers see fully-old or fully-new, never partial) and `/Users/ove/git/cboard2/src/cboard2/tui.py:868` (`CboardApp._pulling` — every read/write confirmed marshaled to the UI thread; no worker touches it directly).
   Runs at once: unshaped — no concurrent execution touches these unsafely; kept for completeness rather than dropped. | Lens: `shared-mutable-state`.

### Skipped
- 3 findings dropped as confirmed non-issues, filed here rather than as action items: lock-discipline's #2 (`call_from_thread` confirmed as the sole correctly-used cross-thread serialization primitive for all `CboardApp` state, `tui.py:857-1221` — no defect found) and #3 (grep-confirmed zero locks exist in `src/cboard2/`, correctly deferred to the other lenses); and check-then-act's #7 (`gitstate.py:208-239` — `Poller.poll` confirmed to have a single caller, `board.py:121`, called only from the "poll" group; no second execution exists).
- 0 findings discarded as hallucinated — every file:line citation from all four reviewers, plus the Textual internals lock-discipline cited (`worker.py`, `worker_manager.py`, `app.py`), matched the actual code.
- 1 finding demoted to Green as `unshaped`: the `_ancestry`/`_origin_urls` memoization race (item 8), demoted from check-then-act's Medium vote — the interleaving is real and named, but the writes are idempotent and harmless on this shape.
- 0 reviewer-reported Criticals remapped to High — the one Critical that survived (item 1) describes a genuine silent overwrite with nothing raised, satisfying Rule B.
- Reviewers run: shared-mutable-state, check-then-act, lock-discipline, task-lifecycle (Quick mode, all 4). No reviewers were skipped or reweighted.
- Validation correction (not a reviewer error in citation, but in consequence): task-lifecycle's findings #1, #2, and #4 described uncaught worker exceptions as causing a silent, permanent freeze. Reading Textual's `worker.py:369-384` and `_work_decorator.py:44` shows `exit_on_error` defaults to `True` and is never overridden here, so the actual consequence is an immediate app crash, not a silent stall — corrected in item 5 above.
