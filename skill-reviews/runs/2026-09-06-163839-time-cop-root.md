---
skill: time-cop
mode: Quick
target: root
rev: fadb3ec
dirty: yes
reviewers: tz-storage, boundary-definition, expiry-math, dst-arithmetic
skipped: none — the "no timezone evidence" condition applied uniformly via a pre-run erratum (single host, single zone, reader = writer, confirmed by the user before any reviewer launched), keeping all four Quick-mode lenses running but capping every finding at Low/Green
---

## Time Results

### Red — Fix Now
Wrong data that persists, or an effect dropped or duplicated.

*(none)*

### Yellow — Should Address
An operator can fix it by hand, or two readers will disagree.

*(none)*

### Green — Consider
Correct for this deployment, or hardening with no dated failure. Fix when nearby.

1. [ ] **Offset-less ISO timestamp from `gh` would fall back to host-local interpretation silently** — `_timestamp()` parses PR `updatedAt`/`mergedAt` with `datetime.fromisoformat()` and stores the result as a bare epoch float with no record of whether the source string carried an offset.
   `src/cboard2/remote.py:869-877` (`_timestamp`), `src/cboard2/remotecache.py:188,199` (`_stored`/`_stored_merged` persist the resulting epoch float) — reject or log an offset-less ISO string in `_timestamp()` instead of silently accepting a host-local interpretation.
   Breaks at: undated — no breaking moment on current evidence; the code comment states `gh` does not emit offset-less ISO strings in practice, and per the erratum (single host, single zone, reader = writer, always) nothing would disagree even if it did. | Lens: `tz-storage`

### Below the cap
*(not applicable — only 1 action item survived distillation, well under the 35-item cap)*

### Skipped
- 3 canonical findings (7 reviewer rows merged) dropped as ceremony (no dated failure on this deployment):
  - Rolling-window boundary inclusivity (`>=` at the cutoff, correct half-open-by-construction semantic) — merged from `boundary-definition` #1 and `expiry-math` #4/#5, all pointing at the same call sites: `src/cboard2/cli.py:391-392`, `src/cboard2/tui.py:194-195,920`, `src/cboard2/activity.py:160-161`.
  - Interval `due()` expiry checks (`>=` at the exact interval boundary, errs toward re-reading, never toward accepting stale data) — merged from `expiry-math` #1/#2/#3: `src/cboard2/remote.py:927`, `src/cboard2/gitstate.py:217`, `src/cboard2/board.py:140`.
  - DST test fixture evaluated in UTC, a zone with no transitions — `dst-arithmetic` #1: `tests/test_remote.py:61`.
- 0 findings discarded as hallucinated (all 8 cited file:line references were read and matched their reviewer's description).
- 1 finding demoted to Green as `undated` (no breaking moment named) — the tz-storage hardening item above; it is in the Green list, not dropped.
- 0 reviewer-reported Criticals remapped to High (all four reviewers reported Low across the board; no Critical, High, or Medium findings were filed by any lens).
- Reviewers run: tz-storage, boundary-definition, expiry-math, dst-arithmetic. Reviewers skipped or reweighted: none — the "no timezone evidence" condition applied uniformly via the pre-run erratum (single host, single zone, reader = writer), keeping all four lenses running but capping every finding at Low/Green.

Want to start working on any of these items?
